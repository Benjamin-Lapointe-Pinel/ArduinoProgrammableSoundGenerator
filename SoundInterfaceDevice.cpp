#include "SoundInterfaceDevice.h"

/*int16_t seed = 0x5EED5EED;

void set_noise_seed(int16_t s)
{
  seed = s;
}

int16_t get_noise()
{
  uint16_t r = seed >> 6;
  r = (r & 0x01) ^ (r >> 1);
  seed = (seed << 1) | r;
  return r;
}*/





